import React from 'react'

const ModalDefaultPage = () => {
  return null;  // no visible content is rendered on this page if we came from a fresh url, but there will be if we accessed (.)image i.e while being on the site
}

export default ModalDefaultPage